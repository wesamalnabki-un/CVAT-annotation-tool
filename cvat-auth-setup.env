# Backend Configuration
# IMPORTANT: This must be your "Cognito Domain" (Hosted UI), NOT the "User Pool ID" or "Issuer URL".
# Format: https://<your-prefix>.auth.<region>.amazoncognito.com
# Example: https://cvat-auth.auth.us-east-1.amazoncognito.com
COGNITO_DOMAIN=https://disha-test.auth.ap-southeast-1.amazoncognito.com
COGNITO_APP_ID=1nidjq4e29minv6lr452atn5a2
# COGNITO_USER_POOL_ID is not strictly required for the current setup, but good to keep for reference
COGNITO_USER_POOL_ID=ap-southeast-1_oJXHAc2lh

# Frontend Configuration (Must match the values above)
# These are required because the browser needs to access them directly
REACT_APP_COGNITO_DOMAIN=https://disha-test.auth.ap-southeast-1.amazoncognito.com
REACT_APP_COGNITO_APP_ID=1nidjq4e29minv6lr452atn5a2
CVAT_BASE_URL=http://localhost:8080
